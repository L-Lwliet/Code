<table class="table table-bordered">
    <tr>
        <th>Id</th>
        <th>Name</th>
        <th>Dept</th>
    </tr>
    @foreach(var item in Model)
    {
        <tr>
            <td>@item.Id</td>
            <td>@item.Name</td>
            <td>@item.DeptId</td>
        </tr>
    }

</table>