#include<stdio.h>

int main(){
    // int a, b;
    // scanf("%d %d", &a, &b);

    // int q = a/b;
    // int r = a%b;

    // printf("%d %d", r, q);

    long c;
    printf("%lu", sizeof(c));

    return 0;
}